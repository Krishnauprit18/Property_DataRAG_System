# Google Gemini API Key (Optional - get free key from https://makersuite.google.com/app/apikey)
# Leave empty or remove line if you don't have an API key - system will use fallback responses
GEMINI_API_KEY=

# Backend Server Configuration
BACKEND_HOST=localhost
BACKEND_PORT=8000

# Frontend Configuration
BACKEND_URL=http://localhost:8000
